services:
  - type: web
    name: wine-quality-classifier
    runtime: docker
    dockerfilePath: ./Dockerfile

    # Build Args — disponíveis APENAS durante o docker build (dvc pull)
    dockerBuildArgs:
      - key: DAGSHUB_USERNAME
        sync: false
      - key: DAGSHUB_TOKEN
        sync: false

    # Environment variables — disponíveis em RUNTIME
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
      - key: DAGSHUB_USERNAME
        sync: false
      - key: DAGSHUB_REPO_NAME
        sync: false
      - key: DAGSHUB_TOKEN
        sync: false
      - key: ENVIRONMENT
        value: production

    healthCheckPath: /_stcore/health
    healthCheckInterval: 30

    repo: https://github.com/frpbotero/wine_quality
    branch: master
    autoDeploy: true
